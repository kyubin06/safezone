// ============================================================
//  카카오맵 검색 설정
//  아래 따옴표("") 안에 본인의 카카오 REST API 키를 붙여넣으세요.
//  키가 비어 있으면 자동으로 무료 Photon 검색을 사용합니다.
// ============================================================
const KAKAO_REST_KEY = "417485fae195d879d8867e677b4d7237";


// ============================================================
//  🤖 포즈 미션 (MediaPipe Pose + 학습된 MLP) 설정
// ============================================================
//  · 학습 노트북에서 만든 pose_model.json 을 js/ 폴더에 넣으세요.
//  · 판정은 "포즈 유지형": 카메라 보고 자세를 잡아 아래 시간만큼 유지하면 성공.
// ============================================================

// 학습 모델 파일 경로 (js/ 폴더 기준)
const POSE_MODEL_URL = "js/pose_model.json";

// 성공 판정 신뢰도 (0~1). 0.70 = 70% 이상이면 인정
const POSE_THRESHOLD = 0.70;

// 포즈 유지 시간(초). 이 시간 동안 신뢰도 유지 시 성공
const POSE_HOLD_SEC = 2.0;

// 안심경로 위에 등장하는 미션 목록
//  · label 은 pose_model.json 의 labels 와 반드시 일치 (만세 / 왼팔 / 오른팔)
//  · pose 는 가이드 일러스트 종류: both(만세) / left(왼팔) / right(오른팔)
const MISSIONS = [
  {
    label: "만세",              // pose_model.json labels 와 동일
    emoji: "🙌",
    pose: "both",              // 일러스트: 양팔 위로
    title: "만세 포즈를 취해주세요",
    lead: "두 팔을 번쩍 위로!",
    steps: [
      "온몸이 화면에 들어오도록 한두 걸음 물러서요.",
      "두 팔을 머리 위로 쭉 올려 만세 자세를 만들어요.",
      "카메라를 보고 자세를 그대로 유지해요."
    ],
    reward: 30,                // 획득 Safe-Point
    gauge: 8                   // SAFE 게이지 상승량
  },
  {
    label: "왼팔",
    emoji: "🙋",
    pose: "left",             // 일러스트: 왼팔만 위로
    title: "왼팔 들기에 도전해요",
    lead: "왼팔을 위로 쭉!",
    steps: [
      "온몸이 화면에 들어오도록 한두 걸음 물러서요.",
      "왼팔을 머리 위로 곧게 올려요. (오른팔은 편하게 아래로)",
      "카메라를 보고 자세를 그대로 유지해요."
    ],
    reward: 30,
    gauge: 8
  },
  {
    label: "오른팔",
    emoji: "🙋‍♂️",
    pose: "right",            // 일러스트: 오른팔만 위로
    title: "오른팔 들기에 도전해요",
    lead: "오른팔을 위로 쭉!",
    steps: [
      "온몸이 화면에 들어오도록 한두 걸음 물러서요.",
      "오른팔을 머리 위로 곧게 올려요. (왼팔은 편하게 아래로)",
      "카메라를 보고 자세를 그대로 유지해요."
    ],
    reward: 30,
    gauge: 8
  }
];
